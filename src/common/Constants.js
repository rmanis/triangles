
define([], function() {

    var Constants = {
        // NOTE: keep this and the View's grid spacing proportional
        sectorSize: 1000,
        planetBroadcastDelay: 10000,
    };

    return Constants;
});
